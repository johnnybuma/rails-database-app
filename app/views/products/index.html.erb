<div class="container">
  <div class="row">
    <div class="col-md-3">
      <h3>Products</h3>

    </div>
    <div class="col-md-6 search-div form-inline">
    <%= form_tag(products_path, :method => "get", id: "search-form", class: "control-group") do %>
          <%= text_field_tag :search, params[:search], placeholder: "Search Products", class: "form-control form-margin" %>
          <%= submit_tag "Search", class: " btn btn-primary form-margin" %>
    <% end %>

    <% if @products.present? %>
        <%= render @products %>
    <% else %>
        <p>There are no products containing the term(s) <%= params[:search] %>.</p>
    <% end %>
    </div>
  </div>
  <div class="row form-margin">
    <div class="col-lg-12 table-responsive">
     <table class="table">

       <tr>
         <th>Id:</th>
         <th>Name:</th>
         <th class="notable">Size:</th>
         <th class="notable">Color:</th>
         <th class="notable">Weight:</th>
         <th class="notable">Dimensions:</th>
         <th class="notable">Quantity:</th>
         <th class="notable">Location:</th>
         <th class="notable">Description:</th>

         <th>Edit:</th>
       </tr>

       <% @products.each do |product| %>

      <tr>
        <td><%= product[:id] %></td>
        <td><%= product.name %></td>
        <td class="notable"><%= product.size %></td>
        <td class="notable"><%= product.color %></td>
        <td class="notable"><%= product.weight %></td>
        <td class="notable"><%= product.dimensions %></td>
        <td class="notable"><%= product.quantity %></td>
        <td class="notable"><%= product.location %></td>
        <td class="notable"><%= product.description %></td>

        <td><%= link_to("Delete", product_path(product), :data => { :confirm => "Are you sure?" }, :method => :delete, :class => 'button-xs btn-danger') %><span style="margin-left: 5px; margin-right: 5px;"></span><%= link_to("Update", edit_product_path(product), :method=> :get, :class => 'button-xs btn-warning') %></td>


      </tr>
      <%end%> 
      </table>
    </div>
  </div>
</div>